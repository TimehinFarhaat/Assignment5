trigger:
- master

pool:
  vmImage: 'ubuntu-latest'

variables:
  buildConfiguration: 'Release'

  # These get replaced by Azure pipeline secrets
  ASPNETCORE_ENVIRONMENT: 'Production'
  ConnectionStrings__DefaultConnection: "Server=tcp:galleryserver123.database.windows.net,1433;Initial Catalog=GalleryDb;Persist Security Info=False;User ID=GalleryAdmin;Password=gallery_Admin;MultipleActiveResultSets=False;Encrypt=True;TrustServerCertificate=False;Connection Timeout=30;" 
  Cloudinary__CloudName: "doshg2i7s" 
  Cloudinary__ApiKey: "579626518565432" 
  Cloudinary__ApiSecret: "7WbK-P5Wkz8dKko258n8abRwQbE" 

steps:
- task: UseDotNet@2
  inputs:
    packageType: 'sdk'
    version: '7.0.x'
    installationPath: $(Agent.ToolsDirectory)/dotnet

- task: DotNetCoreCLI@2
  inputs:
    command: 'restore'
    projects: '**/*.csproj'

- task: DotNetCoreCLI@2
  inputs:
    command: 'build'
    arguments: '--configuration $(buildConfiguration)'
    projects: '**/*.csproj'

- task: DotNetCoreCLI@2
  inputs:
    command: 'publish'
    publishWebProjects: true
    arguments: '--configuration $(buildConfiguration) --output $(Build.ArtifactStagingDirectory)'
    zipAfterPublish: true

- task: PublishBuildArtifacts@1
  inputs:
    pathToPublish: '$(Build.ArtifactStagingDirectory)'
    artifactName: 'drop'
